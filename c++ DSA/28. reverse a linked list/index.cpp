#include <iostream>
using namespace std;

int main(){
     //this is a question number 206 on leadcode

    //  ListNode*prev = NULL;
    //  ListNode*curr = head;
    //  ListNode*next = NULL;

    //  while(curr!=NULL){
    //     next= curr->next;
    //     curr->next= prev;
    //      prev=curr;
    //      curr=next;
    //  }
    //  return prev;
}