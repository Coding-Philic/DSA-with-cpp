#include <iostream>
using namespace std;

int main(){
    // ll*slow = head;
    // ll*fast = head;

    // while(fast != NULL && fast->next != NULL){
    //     slow = slow->next;
    //     fast = fast->next->next;
    // }
    // return slow;
}